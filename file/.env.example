DEBUG=
# Set to 'True' for development and 'False' for production.
# Example: DEBUG=True

SECRET_KEY=
# A secret key used for cryptographic operations in Django.
# Must be kept private and not exposed in version control.

ALLOWED_HOSTS=
# Comma-separated list of hosts/IPs allowed to serve the Django app.
# Example: ALLOWED_HOSTS=localhost,127.0.0.1,example.com

